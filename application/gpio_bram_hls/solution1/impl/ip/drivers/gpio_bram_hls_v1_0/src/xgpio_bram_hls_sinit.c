// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2015.3
// Copyright (C) 2015 Xilinx Inc. All rights reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xgpio_bram_hls.h"

extern XGpio_bram_hls_Config XGpio_bram_hls_ConfigTable[];

XGpio_bram_hls_Config *XGpio_bram_hls_LookupConfig(u16 DeviceId) {
	XGpio_bram_hls_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XGPIO_BRAM_HLS_NUM_INSTANCES; Index++) {
		if (XGpio_bram_hls_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XGpio_bram_hls_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XGpio_bram_hls_Initialize(XGpio_bram_hls *InstancePtr, u16 DeviceId) {
	XGpio_bram_hls_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XGpio_bram_hls_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XGpio_bram_hls_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

